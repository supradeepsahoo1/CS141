#include <iostream>
#include <cmath>
using namespace std;

int main() {
  double x, y;
  cout << "enter the base and then the index";
  cin << x << y;
  cout << "value of " << x << " raised to the power of " << y << ": " << pow(x, y);
}
